# 개발 환경 설정
VITE_API_BASE_URL=http://localhost:3001

# 프로덕션 환경 (Vercel Rewrites)
# VITE_API_BASE_URL=/api

# 백엔드 서버 URL (Vercel Serverless Function에서 사용)
BACKEND_URL=http://localhost:3001

# Challenge 관련 설정
VITE_CHALLENGE_CACHE_TTL=3600
VITE_MAX_CHALLENGES_PER_SESSION=10

# Vercel 배포시 필수 설정:
# 1. Vercel 대시보드에서 Environment Variables 설정
# 2. VITE_API_BASE_URL을 실제 백엔드 URL로 설정
# 3. 다른 환경변수들도 필요에 따라 설정
